import * as types from '../constants/actionTypes'

const initialState = {
    keys: {},
    options: {},
    path: '',
}

export const router = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}